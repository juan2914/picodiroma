Primero ir a directorio del proyecto

git init 

git add . 

git commit -m "Mensaje descriptivo de la modificacion"

git remote add origin https://github.com/juan2914/picodiroma.git

git push master origin